export default class Shuttle {
  constructor(id, latitude, longitude, speed, routeID) {
    this.id = id;
    this.latitude = latitude;
    this.longitude = longitude;
    this.speed = speed;
    this.routeID = routeID;
  }
}
